rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access by default.
    // All data access should go through the API server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
